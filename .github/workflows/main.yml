name: 🚀 Dev container
on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  build:
    name: Dev Container - Build image
    runs-on: ubuntu-latest
    steps:
      - name: Dev Container Publish
        # You may pin to the exact commit or the version.
        # uses: devcontainers/action
        uses: devcontainers/action@v1.3.1
